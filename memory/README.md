# Validated Memory

Validated AI outputs will live here once the memory layer is implemented.

Generated content must not become reusable knowledge automatically. It should
move through explicit states such as `draft`, `pending`, `validated`,
`rejected`, and `superseded`.
