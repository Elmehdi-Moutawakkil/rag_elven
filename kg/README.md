# Knowledge Graphs

Future per-universe Knowledge Graph exports and databases will live here.

The current runtime still uses:

- `vector_db/knowledge_graph.sqlite`
- `vector_db/terran_empire/knowledge_graph.sqlite`
