# Storage

Raw and processed multimodal assets will live here.

This directory is intentionally separate from `corpus/` and `indexes/`:

- `corpus/` stores versioned source-of-truth manifests and canon notes.
- `storage/` stores raw/processed assets and sidecars.
- `indexes/` stores retrieval artifacts.

The current app does not read this directory yet.
